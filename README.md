# RaumRegionMensch QGIS plugin

Management of triggers in PostGIS database to copy attributes based on spatial relation


## Building package

Run ```package.sh``` to generate a ZIP file with the plugin.
